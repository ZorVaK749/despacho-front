services:
  frontend:
    build: .
    container_name: despachos-frontend
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - frontend-net

networks:
  frontend-net:
    driver: bridge